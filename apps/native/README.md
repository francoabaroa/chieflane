# Chieflane Native

Phase two lives here. The native app should reuse:

- `@chieflane/surface-schema`
- `@chieflane/surface-catalog`
- `@chieflane/surface-renderer-native`

The web PWA remains the primary shell until the React Native registry and push/share-extension work are ready.
